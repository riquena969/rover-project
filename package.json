{
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/index.js",
    "dev": "nodemon --watch src --exec \"npm run build && npm run start\"",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.1.1",
    "jest": "^29.1.2",
    "nodemon": "^2.0.20",
    "ts-jest": "^29.0.3",
    "typescript": "^4.8.4"
  }
}
